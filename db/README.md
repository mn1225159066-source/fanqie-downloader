# 数据库操作说明（MySQL）

- 在宝塔面板中创建数据库：设置库名、用户名、密码，并记录。
- 通过面板的「phpMyAdmin」或「数据库管理」功能，导入 `migrations/V1__init.sql`。
- 生产环境务必：
  - 开启远程访问仅对后端服务器 IP，如果前后端同机则关闭远程访问。
  - 修改用户密码为强密码，并限制权限仅此库。
  - 数据库字符集使用 `utf8mb4`，排序规则建议 `utf8mb4_general_ci` 或按需要调整。

> 后续如需变更结构，建议以 `V2__xxx.sql`、`V3__xxx.sql` 递增的方式维护变更脚本。